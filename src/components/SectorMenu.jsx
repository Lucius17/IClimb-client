/*import React from 'react';
import PropTypes from 'prop-types';

function SectorMenu({ onSelectSector }) {
    return (
        <div className="sector-menu my-3">
            <h5>Wybierz Sektor</h5>
            <div className="btn-group" role="group">
                <button className="btn btn-outline-primary" onClick={() => onSelectSector('sector1')}>Sektor 1</button>
                <button className="btn btn-outline-primary" onClick={() => onSelectSector('sector2')}>Sektor 2</button>
                <button className="btn btn-outline-primary" onClick={() => onSelectSector('sector3')}>Sektor 3</button>
            </div>
        </div>
    );
}

SectorMenu.propTypes = {
    onSelectSector: PropTypes.func.isRequired,
};

export default SectorMenu;
*/